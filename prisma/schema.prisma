generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model EventLog {
  id        Int      @id @default(autoincrement())
  topic     String
  payload   String
  createdAt DateTime @default(now())
}